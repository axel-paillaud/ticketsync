<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}TicketSync{% endblock %}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #3c3836;
            background-color: #f9f5d7;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            background: #fbf1c7;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 4px rgba(60, 56, 54, 0.1);
        }
        .header {
            background: linear-gradient(135deg, #458588 0%, #076678 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 600;
        }
        .content {
            padding: 30px 20px;
        }
        .greeting {
            font-size: 18px;
            margin-bottom: 20px;
        }
        .info-box {
            background: #f9f5d7;
            border-left: 4px solid #458588;
            padding: 20px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .info-box h2 {
            margin-top: 0;
            font-size: 20px;
            color: #458588;
        }
        .info-box p {
            margin: 8px 0;
        }
        .info-box strong {
            color: #504945;
        }
        .description {
            background: #f9f5d7;
            border: 1px solid #d5c4a1;
            padding: 15px;
            margin: 15px 0;
            border-radius: 4px;
            white-space: pre-wrap;
            word-wrap: break-word;
        }
        .button {
            display: inline-block;
            padding: 12px 24px;
            background: #458588;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin: 20px 0;
            font-weight: 500;
        }
        .button:hover {
            background: #076678;
        }
        .footer {
            background: #f9f5d7;
            padding: 20px;
            text-align: center;
            color: #928374;
            font-size: 14px;
            border-top: 1px solid #d5c4a1;
        }
        .footer p {
            margin: 5px 0;
        }
        .footer a {
            color: #458588;
            text-decoration: none;
        }
        .footer a:hover {
            text-decoration: underline;
        }
        .badge {
            display: inline-block;
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }
        .badge-status {
            background: #f9f5d7;
            color: #076678;
        }
        .badge-priority {
            background: #f9f5d7;
            color: #b57614;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎫 TicketSync</h1>
        </div>
        <div class="content">
            {% block content %}{% endblock %}
        </div>
        <div class="footer">
            <p>{{ 'This is an automated email, please do not reply.'|trans }}</p>
            <p>&copy; {{ 'now'|date('Y') }} TicketSync - {{ 'Ticket Management System by'|trans }} <a href="https://axelweb.fr/">Axelweb</a></p>
        </div>
    </div>
</body>
</html>
