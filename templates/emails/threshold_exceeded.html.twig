{% extends 'emails/_layout.html.twig' %}

{% block title %}Monthly threshold exceeded{% endblock %}

{% block content %}
    <p class="greeting">Hello <strong>{{ recipient.name }}</strong>,</p>

    <p>Your monthly time entry threshold has been exceeded for <strong>{{ organization.name }}</strong>.</p>

    <div class="info-box">
        <h2>⚠️ Threshold Alert</h2>
        <p><strong>Organization:</strong> {{ organization.name }}</p>
        <p><strong>Your threshold:</strong> {{ threshold|number_format(2, '.', ',') }} €</p>
        <p><strong>Current monthly total:</strong> <span style="color: #d9534f; font-weight: bold;">{{ currentTotal|number_format(2, '.', ',') }} €</span></p>
        <p><strong>Exceeded by:</strong> {{ (currentTotal - threshold)|number_format(2, '.', ',') }} €</p>
    </div>

    <p>This alert is sent once when your monthly billing total exceeds your configured threshold.</p>

    <p>You can adjust your alert settings in your profile.</p>

    <a href="{{ url('app_user_profile') }}" class="button">
        Go to Profile
    </a>
{% endblock %}
